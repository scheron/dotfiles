.DS_Store
*.bak

# Never commit secrets or app-generated state, even if a symlinked app
# writes them back into the repo tree.
gh/hosts.yml
claude/.claude.json
**/automatic_backups/
zed/conversations/
zed/*.mdb
zed/settings_backup.json
